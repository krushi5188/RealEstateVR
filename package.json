{
  "name": "vr-floor-plan",
  "version": "1.0.0",
  "description": "A web application that converts 2D floor plans into interactive VR experiences.",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently --names \"SERVER,CLIENT\" -c \"bgBlue.bold,bgGreen.bold\" \"npm start --prefix server\" \"npm start --prefix client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jimp": "^1.6.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
