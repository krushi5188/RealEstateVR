name: Build ArchNative Release

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build-macos:
    runs-on: macos-13 # Intel Mac (x64)
    timeout-minutes: 60
    env:
      HOMEBREW_NO_AUTO_UPDATE: 1
      HOMEBREW_NO_INSTALL_CLEANUP: 1
      VCPKG_ROOT: /usr/local/share/vcpkg
    steps:
    - uses: actions/checkout@v4

    # REMOVED: jurplel/install-qt-action (Causes Version Mismatch)

    - name: Install Dependencies (Homebrew)
      run: |
        # We install qt@6 here explicitly. OpenCV will use this same version.
        brew install opencv tesseract cmake pkg-config vulkan-headers qt@6
        
        # Set Environment variables for Homebrew Qt
        echo "QT_ROOT_DIR=$(brew --prefix qt@6)" >> $GITHUB_ENV
        echo "Qt6_DIR=$(brew --prefix qt@6)/lib/cmake/Qt6" >> $GITHUB_ENV
        
        # Add Qt bin to PATH so 'moc' and 'macdeployqt' are found
        echo "$(brew --prefix qt@6)/bin" >> $GITHUB_PATH

    - name: Install OpenXR (Vcpkg)
      run: |
        $VCPKG_INSTALLATION_ROOT/vcpkg install openxr-loader --triplet=x64-osx

    # --- CRITICAL FIXES FOR COMPILATION ---
    - name: Patch C++ Code for macOS Platform
      run: |
        echo "Patching source for macOS..."
        
        # 1. Force Mac Platform Macro (stops looking for Linux X11)
        sed -i '' 's/XR_USE_PLATFORM_XLIB/XR_USE_PLATFORM_MACOS/g' ArchNative/src/Core/OpenXRManager.cpp || true
        
        # 2. Swap Linux Header for Mac Header
        sed -i '' 's|#include <GL/glx.h>|#include <OpenGL/gl.h>|g' ArchNative/src/Core/OpenXRManager.cpp || true
        
        # 3. SAFETY NET: Inject fake X11 typedefs just in case code uses them directly
        # This prevents "unknown type name 'Display'" errors
        sed -i '' '1i\
        #if defined(__APPLE__)\
        typedef void* Display;\
        typedef void* GLXFBConfig;\
        typedef void* GLXDrawable;\
        typedef void* GLXContext;\
        #endif\
        ' ArchNative/src/Core/OpenXRManager.cpp
        
        # 4. Patch CMake target name for OpenXR
        sed -i '' 's/OpenXR::OpenXR/OpenXR::openxr_loader/g' ArchNative/CMakeLists.txt || true
        
        echo "Patches applied."

    - name: Configure CMake
      run: |
        # We point CMake to the Homebrew Qt we set up in env vars
        cmake -S ArchNative -B build \
          -G "Unix Makefiles" \
          -DCMAKE_PREFIX_PATH="$QT_ROOT_DIR" \
          -DCMAKE_TOOLCHAIN_FILE="$VCPKG_INSTALLATION_ROOT/scripts/buildsystems/vcpkg.cmake" \
          -DVCPKG_TARGET_TRIPLET=x64-osx \
          -DCMAKE_CXX_FLAGS="-DGL_SILENCE_DEPRECATION -DXR_USE_PLATFORM_MACOS -DXR_USE_GRAPHICS_API_OPENGL"

    - name: Build
      run: cmake --build build --config Release

    # --- CRITICAL FIX FOR DEPLOYMENT (Strategy B) ---
    - name: Package DMG (Homebrew Fix)
      run: |
        echo "Preparing to package..."
        QT_LIB_DIR="$(brew --prefix qt@6)/lib"
        APP_PATH="build/ArchNative.app"
        EXE_PATH="$APP_PATH/Contents/MacOS/ArchNative"
        
        # 1. Manual RPATH Injection (Helps macdeployqt find libs)
        install_name_tool -add_rpath "$QT_LIB_DIR" "$EXE_PATH" || true
        
        # 2. Run macdeployqt with explicit library path
        # -libpath tells it exactly where to look for the framework dependencies
        macdeployqt "$APP_PATH" -dmg -verbose=1 -libpath="$QT_LIB_DIR"

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: ArchNative-macOS
        path: build/*.dmg

  build-windows:
    runs-on: windows-latest
    timeout-minutes: 180
    steps:
    - uses: actions/checkout@v4

    - name: Install Qt
      uses: jurplel/install-qt-action@v4
      with:
        version: '6.5.0'
        host: 'windows'
        target: 'desktop'
        arch: 'win64_msvc2019_64'

    - name: Cache Vcpkg
      id: cache-vcpkg
      uses: actions/cache@v4
      with:
        path: |
          C:\vcpkg\installed
          C:\vcpkg\packages
          C:\vcpkg\downloads
        key: ${{ runner.os }}-vcpkg-opencv-tesseract-openxr-v1

    - name: Install Dependencies (Vcpkg)
      if: steps.cache-vcpkg.outputs.cache-hit != 'true'
      run: |
        vcpkg install opencv:x64-windows
        vcpkg install tesseract:x64-windows
        vcpkg install openxr-loader:x64-windows
        vcpkg install jsoncpp:x64-windows
        vcpkg integrate install

    - name: Configure CMake
      run: cmake -S ArchNative -B build -DCMAKE_PREFIX_PATH=$env:QT_ROOT_DIR -DCMAKE_TOOLCHAIN_FILE=C:/vcpkg/scripts/buildsystems/vcpkg.cmake

    - name: Build
      run: cmake --build build --config Release

    - name: Package
      run: |
        windeployqt build/Release/ArchNative.exe --dir dist
        Copy-Item -Path "C:/vcpkg/installed/x64-windows/bin/*.dll" -Destination "dist/" -Force
        echo "Dist folder contents:"
        ls dist

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: ArchNative-Windows
        path: dist/
